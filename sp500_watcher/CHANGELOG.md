# 変更履歴 (CHANGELOG)

バージョンは `APP_VERSION`（sp500_watcher.py 冒頭）で管理し、ヘッダー右上とウィンドウタイトルに `v<バージョン>` を表示する。配布zipも `sp500_watcher_mac_arm64_v<バージョン>.zip` の形で揃える。

## v2026.06.21
- **オルカン(2559.T)表記の崩壊を恒久修正**: yfinanceの分割過渡期データ（未調整の×10水準・分割後水準・単発の1/10誤ティックが混在）に対し、
  - `fetch_quote`: 前日終値を「当日価格と同じ桁(1/3〜3倍)に収まる直近終値」から選ぶ外れ値耐性ロジックに変更（固定比率ヒューリスティック依存を廃止）。
  - `_unsplit_adjust`: 段差検出方式をやめ、「現在水準(直近5点の中央値)を基準に各点を正規化＋単発外れ値の平滑化」に変更。混在データでも破綻しない。
- バージョン表示の追加（ヘッダー右上・ウィンドウタイトル）と本CHANGELOGによる管理を開始。

## v2026.06.16
- **ポートフォリオ合計の約2%ずれを根治**: 破壊的な日次複利更新を撤廃し、`est = 入力時評価額 ×(入力時起点のベンチマーク累積騰落)×(為替累積)` を毎回再計算する非破壊設計へ変更。
  - バックエンドに入力時終値取得（`fetch_origin_close` / `origin_closes` / `POST /api/origin_closes`）を追加。
  - 合計ロジックを `totalEst += est!=null ? est : base` に変更し、ベンチマーク一時失敗時も銘柄が合計から脱落しないよう堅牢化。
  - `cumPct` に分割規模(>5倍/<0.2倍)の異常比率ガードを追加。

## v2026.06.07
- オルカン株式分割(1→10, 2026-06-05)の暫定補正（`SPLIT_OVERRIDES`）を追加し、前日比-89%の暴落誤表示を修正。
- ポートフォリオに現金預金欄を追加。
</content>
</invoke>
